<UserControl x:Class="Autoservis.Views.AutoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Autoservis.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation = "Horizontal">
            <ListView Margin="10" ItemsSource="{Binding Path=Auta}" HorizontalAlignment="Left" HorizontalContentAlignment="Left">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Značka"  DisplayMemberBinding="{Binding Path= ZnackaVozu}" />
                        <GridViewColumn Header="Model"  DisplayMemberBinding="{Binding Path= ModelVozu}" />
                    </GridView>
                </ListView.View>
            </ListView>
            <StackPanel Orientation="Vertical">

                <Button Content="Přidat" Height="30" HorizontalAlignment="Left" Margin="15,10,0,0" VerticalAlignment="Top" Width="100"/>
            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>
